# Cloudflare Pages 配置文件
# 注意：主要部署通过 GitHub Actions 完成，此文件用于本地开发和高级配置

name = "gimson-phonetics"
pages_build_output_dir = "dist"
compatibility_date = "2024-01-01"

# 环境变量（生产环境）
[vars]
NODE_ENV = "production"

# 开发环境配置
[dev]
port = 8788
local_protocol = "http"
